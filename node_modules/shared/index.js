var fs = require('fs');
module.exports = {};

var shared_path = __dirname + '/bin';
fs.readdirSync(shared_path).forEach(function (file) {
    if (~file.indexOf('.js')) {
        module.exports[ file.substring(0, file.length - 3) ] = require(shared_path + '/' + file);
    }
});